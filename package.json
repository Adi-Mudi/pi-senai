{
  "name": "pi-senai",
  "version": "0.1.0",
  "description": "Senai — stage-gated agent orchestration extension for Pi — Plan → Implement → Document → Deliver",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourname/pi-senai"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/pi-extension/test/**/*.test.js"
  },
  "pi": {
    "extensions": [
      "pi-extension/src/index.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@mariozechner/pi-tui": "^0.73.1",
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
